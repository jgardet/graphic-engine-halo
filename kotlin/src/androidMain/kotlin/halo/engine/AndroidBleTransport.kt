package halo.engine

// Android implementation lives in the :android library module.
// This source-set placeholder is intentionally empty so the JVM module does
// not define a second AndroidBleTransport class.
